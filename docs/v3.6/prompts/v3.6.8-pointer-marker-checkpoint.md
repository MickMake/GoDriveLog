# v3.6.8 Pointer Marker Docs Checkpoint

Implement the v3.6.8 slice only.

This file is one v3.6 implementation slice.

If invoked from a broad instruction such as "implement the next slice", first confirm this file matches the current target or the first unchecked allowed slice in `docs/v3.6/ImplementationState.md`.

Do not implement this slice if v3.6.7 is still unchecked.

## Goal

Finalise the v3.6 pointer marker work and confirm the release stayed scoped to pointer markers only.

This is primarily a checkpoint and documentation slice.

## Requirements

- Review `docs/v3.6/ImplementationState.md` and `docs/v3.6/ReleasePlan.md`.
- Confirm radial max/min pointer markers are documented and tested.
- Confirm bar max/min pointer markers are documented and tested.
- Confirm damped secondary pointer marker semantics are documented as visual/mechanical behaviour, not arithmetic averaging.
- Confirm reset/session behaviour is documented.
- Confirm pointer markers remain display-only and do not mutate source values, logs, exports, configured ranges, or input data.
- Confirm no odometer, numeric, segmented, indicator, or broad realism audit work was added to v3.6.
- Move any newly discovered non-marker follow-up ideas to v3.7+ docs or a separate issue/PR.

## Do not

- Do not implement backlog items during this checkpoint unless the user explicitly asks for a separate follow-up release.
- Do not mix mathematical statistics overlays into `realism.pointer_markers`.
- Do not add persistence casually.
- Do not add odometer backlash or other non-marker realism work to v3.6.
- Do not promote new v3.6 slices unless the user explicitly changes the release scope.

## Completion

Update `docs/v3.6/ImplementationState.md` to mark v3.6.8 complete and set `Current target` to `none` only after the pointer marker docs, tests, and relevant checks are complete.
