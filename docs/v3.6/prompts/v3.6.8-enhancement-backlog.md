# v3.6.8 Enhancement Backlog Triage

Implement the v3.6.8 slice only.

This file is one v3.6 implementation slice.

If invoked from a broad instruction such as "implement the next slice", first confirm this file matches the current target or the first unchecked allowed slice in `docs/v3.6/ImplementationState.md`.

Do not implement this slice if v3.6.7 is still unchecked.

## Goal

Review the remaining v3.6 enhancement candidates and decide what, if anything, should be promoted into additional v3.6 slices.

This is primarily a planning slice. It exists so future enhancements are captured deliberately rather than smuggled into renderer work wearing a false moustache.

## Requirements

- Review `docs/v3.6/ImplementationState.md` and `docs/v3.6/ReleasePlan.md`.
- Review any shelved branches or prior PRs that were intentionally kept as context for v3.6 planning.
- Confirm which backlog items remain parked.
- Promote only small, well-scoped enhancements into new prompt files.
- Keep promoted slices independent and testable.
- Update `docs/v3.6/ImplementationState.md`, `docs/v3.6/ReleasePlan.md`, and `docs/v3.6/prompts/README.md` if new slices are added.

## Candidate backlog items

- explicit manual marker reset control;
- optional marker labels/tooltips;
- marker preview fixtures for all gauge families;
- indicator marker equivalents;
- numeric/segmented baseline preview improvements;
- mathematical statistics overlays, separately named and scoped;
- persistent marker state;
- marker styling overrides beyond default assets;
- other gauge enhancements identified after v3.6.0.

## Do not

- Do not implement backlog items during triage unless the user explicitly asks to promote and implement one.
- Do not mix mathematical statistics overlays into `realism.pointer_markers`.
- Do not add persistence casually.
- Do not make v3.6 an unbounded wishlist.

## Completion

Update `docs/v3.6/ImplementationState.md` to mark v3.6.8 complete if no further slices are promoted, or append promoted slices and advance `Current target` accordingly.
