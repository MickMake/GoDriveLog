# v3.2.1 prompt: gauge package loader

## Goal

Implement loading of self-contained gauge packages from `assets/gauges/**/gauge.yaml`.

## Required reading

- `docs/v3.2/README.md`
- `docs/v3.2/ReleasePlan.md`
- `docs/v3.2/ImplementationState.md`
- `docs/v3.2/OpenDecisions.md`
- `docs/v3.2/CarryForward.md`

## Branch

Use a branch named:

```text
v3.2.1-gauge-package-loader
```

Branch from latest `main` after confirming the v3.2.0 planning PR is merged.

## Scope

Add the minimal data model and loader support for gauge packages.

A gauge package is any directory under `assets/gauges/` containing a file named `gauge.yaml`.

Directory names under `assets/gauges/` carry no semantic meaning.

## Behaviour

The loader should:

- accept an asset-root relative gauge path such as `gauges/classic/rpm`;
- resolve it to `assets/gauges/classic/rpm/gauge.yaml`;
- parse the gauge YAML;
- support at least `type: radial`;
- preserve sensor binding from `sensor`;
- resolve layer image paths relative to the `gauge.yaml` directory;
- allow relative paths like `../images/bezel.png` when they remain inside the asset root;
- reject path traversal outside the asset root;
- return clear validation/load errors.

## Minimal gauge.yaml shape

```yaml
id: rpm
type: radial
sensor: engine_rpm

size:
  width: 512
  height: 512

layers:
  background: ../images/bezel.png
  face: ../images/face_dark.png
  ticks: ticks.png
  needle: ../images/needle_red.png
  overlay: ../images/glass_overlay.png

pivot:
  face: { x: 0.5, y: 0.55 }
  needle: { x: 0.5, y: 0.9 }

value_map:
  min: 0
  max: 8000
  start_angle: -135
  end_angle: 135
  clamp: true
```

## Out of scope

- Dashboard widget config changes.
- Rendering.
- Fyne work.
- Example assets.
- Sensor override.
- Gauge inheritance.
- Clusters.

## Acceptance checks

- Loader tests cover a valid radial gauge package.
- Loader tests cover arbitrary directory names under `assets/gauges/`.
- Loader tests cover relative shared image paths.
- Loader tests reject missing `gauge.yaml`.
- Loader tests reject paths escaping the asset root.
- `CHANGES.md` is updated.
- `docs/v3.2/ImplementationState.md` is updated.
