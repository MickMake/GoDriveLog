# v3.5.6 Odometer Eased Roll

Implement the v3.5.6 slice only.

This file is one v3.5 implementation slice.

If invoked from a broad instruction such as "implement the next slice", first confirm this file matches the first unchecked slice in `docs/v3.5/ImplementationState.md`.

Do not implement this slice if an earlier slice is still unchecked.

## Goal

Apply finite movement/easing to odometer wheel movement.

## Requirements

- Preserve `movement: smooth` and `movement: click` as base odometer modes.
- Layer easing on top of the existing movement model.
- Use the shared lifecycle and movement policy from earlier slices.
- Add tests for enabled easing, disabled/default behaviour, and final settled value.
- Add a single-feature visual inspection fixture.

## Do not

- Do not add carry-drag, backlash, snap/settle, or wraparound changes beyond what already exists.
- Do not add new easing families unless explicitly required.

## Completion

Update `docs/v3.5/ImplementationState.md` to mark v3.5.6 complete and set the next target to v3.5.7.
